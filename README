Requirements to run the app:
Node.js - Created using the latest LTS version (v22.13.0)

Run npm install to install the dependencies for the tests

To run the app with the simulated scenarios, run the following command:
```node app.js```

To run the tests, run the following command:
```npm test```